# Local wrangler/dev always previews. Live publishing is the Cloudflare Worker
# (wrangler.toml DRY_RUN). Same secret names as Worker secrets / .dev.vars.
# GitHub Actions list/delete/smoke still use these keys; they do not publish.
# X OAuth 1.0a (Worker live post + account maintenance)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
# Required for drafting (Zhipu BigModel Anthropic-compatible); no fallback posts
# Default model: GLM-5.3-Flash
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
# LLM draft (BigModel Anthropic-compatible)
DRAFT_MODEL=glm-5.3-flash
# DRAFT_TIMEOUT_MS=120000
# Explicit thinking is opt-in; omitted means the provider chooses its default.
# DRAFT_THINKING=on  # optional; omit by default
# DRAFT_REASONING_EFFORT=low
# Token exhaustion fails generation. Validate budget changes with smoke:llm before live use.
# Default in code is 32768 if unset. Worker secrets override per request, not at module load.
# DRAFT_MAX_TOKENS=32768
